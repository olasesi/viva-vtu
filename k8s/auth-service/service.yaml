apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: viva-vtu
  labels:
    app: auth-service
    component: api
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP